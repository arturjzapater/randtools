export as namespace normal

export function choose(array: any[], devs?: number): any

export function mean(mean: number, deviation: number, decimals?: number): number

export function range(from: number, to: number, decimals?: number, deviations?: number): number
